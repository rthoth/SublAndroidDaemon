initscript {
	dependencies {
		classpath files(
			'../../build/libs/sublandroid-core.jar',
			'../../build/libs/sublandroid-plugin.jar'
		)
	}
}

allprojects {
	apply plugin: sublandroid.plugin.BuildStatusPlugin
}